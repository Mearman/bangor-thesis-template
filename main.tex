% !TeX program = pdflatex
%%% Bangor University thesis and dissertation template.
%%% Replace the metadata below, then write your chapters under
%%% content/chapters/, one numbered folder per chapter with an index.tex
%%% and a file per section, and add your sources to references.bib.
% Layout options besides the degree keyword. These are on by default and each takes false to turn it off, except sectionfloats, which takes none: pageofm prints "Page N of M", appendixpages numbers each appendix separately as "Appendix A - Page n of m", sharedfloats puts figures, tables and listings on one counter, and sectionfloats numbers floats within their section (sectionfloats=zero numbers a float before the first section of a chapter as 5.0.1 instead of 5.1). headers=chapter puts the page number in the header instead of the footer, and headrule and footrule each take hairline (the default), visible or none.
\documentclass[phd,12pt,a4paper]{bangorthesis}

% Citation style: ieee is the default. Any biblatex style name works.
% Ask your supervisor which style your school requires.
\bibliographysetup
\addbibresource{references.bib}

% Document facts. School and college are free text: use the names your school currently publishes, exactly as they should appear.
\title{Your Thesis Title}
\shorttitle{Short Thesis Title}
\author{Your Name}
\degreescheme{Doctor of Philosophy}
\school{School of Computer Science and Engineering}
\college{College of Science and Engineering}
\supervisor{Dr First Supervisor}
\supervisor{Dr Second Supervisor}
\submissiondate{September 2026}

% Generative AI statement: replace the guidance with your own statement, checked against your school's current policy. Delete this command to keep the built-in guidance text instead.
\aistatement{Describe how you used generative AI tools, or state that you did not use them.}

% Tables and abbreviations are opt-in modules. Load bangortables only if your document uses longtable, tabularx or landscape tables, and bangorglossary only if you want a list of abbreviations.
\usepackage{bangortables}
\usepackage{bangorglossary}

% Abbreviations. Register yours here: \useabbrev expands each one on first use, and \printabbrevs prints the list in the front matter.
\addabbrev{SNR}{signal-to-noise ratio}

% Section continuation markers: the asection environment inserts "(Continued)" when a nested section closes and parent content resumes. Remove this line if you do not want them.
\input{continuation-markers}

% Release builds stamp the template version here. Leave this line as it is; the file appears only in tagged releases.
\InputIfFileExists{version.tex}{}{}

% Code listings for the demo in the methods chapter. Adjust the style
% here; see the listings package documentation for the full option set.
\usepackage{listings}
\lstset{basicstyle=\ttfamily\small, breaklines=true, frame=single}

% Cross-references use cleveref: \cref{label} mid-sentence produces a
% lowercase type name ("see figure 1"), \Cref{label} at a sentence
% opening produces uppercase ("Figure 1 shows"). No regulation mandates
% a reference style, so the class does not impose one; use whichever
% your subject convention prefers.

\begin{document}

\maketitle
\statementspage

\input{content/preliminaries/acknowledgements}
\begin{abstract}
% Single spaced, capped at 600 words by the regulation. The class
% checks the cap and fails the build if the abstract is too long.
Replace this text with your abstract. State the problem, the method,
the results, and the conclusion. Write it last, when the rest of the
document is stable.
\end{abstract}

\tables
\printabbrevs

\input{content/chapters/01-introduction/index}
\input{content/chapters/02-background/index}
\input{content/chapters/03-methods/index}
\input{content/chapters/04-conclusions/index}

\printbibliography

% Appendices follow the bibliography. \appendix letters the chapters that come after it.
\appendix
\input{content/appendices/01-supplementary/index}

\end{document}
