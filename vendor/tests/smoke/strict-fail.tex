% !TeX program = pdflatex
%%% Strict-mode negative smoke test: serif font below the 12pt floor.
%%% In strict mode (the default) this document must FAIL to compile.
%%% With strict=false it must compile with a warning. CI asserts both
%%% behaviours.
\documentclass[phd,font=termes,10pt,a4paper]{bangorthesis}

\title{Strict Mode Failure Smoke Test}
\author{D. Student}
\degreeScheme{Doctor of Philosophy}

\begin{document}
\maketitle
\chapter{Introduction}
This document must fail in strict mode, because serif body text at
10pt is below the regulation floor of 12pt.
\end{document}
